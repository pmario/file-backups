{
	"name": "file-backups",
	"version": "0.1.5",
	"description": "This addOn should give you the possibility to save \"file-based\" TiddlyWikis, to your browser backup directory",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"prebuild": "node copy-version.js",
		"build": "webpack",
		"version": "npm run build"
	},
	"license": "CC-BY-NC-SA",
	"devDependencies": {
		"webpack": "^2.3.2",
		"jsonfile": "^4.0.0"
	},
	"dependencies": {}
}
