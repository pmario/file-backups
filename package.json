{
	"name": "file-backups",
	"version": "0.1.2",
	"description": "This browser add on is designed to let you easily save and backup TiddlyWikis.",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"prebuild": "node copy-version.js",
		"build": "webpack",
		"version": "npm run build",
		"postversion": "node prep-for-signing.js"
	},
	"license": "CC-BY-NC-SA",
	"devDependencies": {
		"webpack": "^2.3.2",
		"jsonfile": "^4.0.0",
		"zip-dir": "^1.0.2"
	},
	"dependencies": {}
}
